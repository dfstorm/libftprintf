#include "libft.h"

int		ft_convert_baseten(int mode, ...)
{
	va_list			arg;
	unsigned int	io;
	unsigned int	nb;
	va_start(arg, mode);
	
	if (mode == 8)
	{
		io = va_arg(arg, unsigned int);
		
		// int to char
		if(nb) {}
		
		
		if (io) {}
	}
	
	va_end(arg);
	if(mode) {}
	return (0);
}
